@model PersonelGorevViewModel
@{
    ViewData["Title"] = "PersonelGorevlendir";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="row mt-4">
    <div class="col-md-4">
        <div class="card mx-auto" style="width: 18rem;">
            <div class="card-header">
                <h4> @Model.Gorev.Ad </h4>
            </div>
            <div class="card-body">

                <p class="card-text">@Model.Gorev.Aciklama</p>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">@Model.Gorev.Aciliyet.Tanim</li>
            </ul>
        </div>
    </div>
    <div class="col-md-8">
        <div class="card mb-3">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="/img/@Model.AppUser.Picture" class="card-img img-fluid" style="height:10rem;" />
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">@Model.AppUser.Name @Model.AppUser.Surname</h5>
                        <p class="card-text"><getirGorevileAppUserId app-user-id="@Model.AppUser.Id"></getirGorevileAppUserId></p>
                        <p class="card-text">
                            <small class="text-muted">@Model.AppUser.Email</small>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-12 text-center">
        <form asp-action="AtaPersonel" method="post">
            <input type="hidden" name="AppUserId" value="@Model.AppUser.Id" />
            <input type="hidden" name="GorevId" value="@Model.Gorev.ID" />
            <p class="lead">
                Yukarıda bulunan göreve yukarıdaki personel atanacaktır.Emin misiniz?
            </p>
            <a asp-action="AtaPersonel" asp-route-id="@Model.Gorev.ID" class="btn btn-danger btn-sm">Vazgeç</a>
            <button class="btn btn-success btn-sm">Görevlendir</button>
        </form>
    </div>
</div>
